This folder stores original audio assets that are kept in the repository for archival/reference purposes.

- Files in `audio_storage/original_sounds/` are not bundled into the app.
- The app uses the compressed `.m4a` files stored under `src/assets/sounds/`.
- The original `.wav` / `.mp3` files remain here as source material.
